apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  labels:
    app: {{ .Chart.Name }}
    component: {{ .Values.component }}
data:
  component: {{ .Values.component | quote }}
  order: {{ .Values.order | quote }}
  chart: {{ .Chart.Name | quote }}
