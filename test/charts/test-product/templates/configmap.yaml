apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  namespace: {{ .Values.namespace }}
  labels:
    app: {{ .Chart.Name }}
    product: {{ .Values.productName | quote }}
data:
  productName: {{ .Values.productName | quote }}
  namespace: {{ .Values.namespace | quote }}
