apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  labels:
    app: {{ .Chart.Name }}
    weight: "{{ .Values.weight }}"
data:
  weight: {{ .Values.weight | quote }}
  expectedOrder: {{ .Values.expectedOrder | quote }}
